/* const devConfig = {
    selfContextIdentifier: "$this"
};


import { join } from "path";

import { ENV.PATH } from "../../ENV.PATH";


export class Plugin {
    
    private readonly name: string;

    constructor(path: string) {
        require(join(ENV.PATH, path));  // TODO: Work with a common base path (e.g. ./plugins/*)?
        // or ENV.PATH.join(ENV.PATH, )?
        
        // TODO: Define $this
    }

} */