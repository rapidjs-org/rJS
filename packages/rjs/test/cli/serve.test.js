// TODO: Enhance CLI testing for partial stdout/-err matching

/* new CLITest("Build with file emitter")
.actual(require("path").join(__dirname, "../../rjs.sh"), [ "serve", "-D", "-W", require("path").join(__dirname, "../../../../test-app") ])
.expected({
    stdout: [
        "..."
    ]
}); */