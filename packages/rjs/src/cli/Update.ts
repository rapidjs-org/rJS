export class Update {
    public static isAvailable(packageReference: string): boolean {
        // TODO: Check if package update is available
        return false;
    }

    constructor(packageReference: string) {
        // TODO: Update package to latest version (prompt)	
    }
}